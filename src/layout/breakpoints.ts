/**
 * Width threshold (logical pixels / dp) at or above which the app uses the
 * tablet layout. See specs/003-edit-view-nav/research.md §1 for the rationale
 * and the device-width table this value was chosen against.
 */
export const TABLET_MIN_WIDTH = 600;
